{
  "//": "Standalone Node service (no bundler) — uses NodeNext instead of the monorepo-wide Bundler resolution.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*.ts"],
  "exclude": ["dist", "tests"]
}
