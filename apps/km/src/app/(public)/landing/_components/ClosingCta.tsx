import { CtaRow } from "./CtaRow";

export function ClosingCta() {
  return (
    <section className="mk-cta-band">
      <h2 className="mk-cta-band-h">Bring your research into one place.</h2>
      <p className="mk-cta-band-sub">
        Episteme is in limited beta. Invite only for now.
      </p>
      <CtaRow />
    </section>
  );
}
